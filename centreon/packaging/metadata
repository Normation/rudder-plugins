{
  "type": "plugin",
  "name": "${plugin-id}",
  "description": "Integration with Centreon (monitoring tool)",
  "version": "${plugin-version}",
  "build-date": "${maven.build.timestamp}",
  "build-commit": "${commit-id}",
  "depends": {
    "apt": [ "python3-requests" ],
    "rpm": [ "python3-requests" ]
  },
  "content": {
    "files.txz": "/opt/rudder/"
  }
}
